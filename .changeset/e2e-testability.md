---
"@jolly-pixel/runtime": minor
"@jolly-pixel/voxel.renderer": minor
"@jolly-pixel/pixel-draw.renderer": minor
"@jolly-pixel/three": minor
---

Add `Runtime.nextFrame()`/`frames(count)`, `VoxelEngine.whenIdle()`, `projectToClient()` in three,
and `textureClientPosition()` on the pixel-draw canvas viewport (typed `CanvasViewport`).
