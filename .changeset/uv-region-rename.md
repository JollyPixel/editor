---
"@jolly-pixel/pixel-draw.renderer": minor
---

Add `UVMap.rename()` and `UVRegion.renamed()` to rename a UV region in place.
The voxel-model editor uses it to keep a block's UV label in sync with its name.
