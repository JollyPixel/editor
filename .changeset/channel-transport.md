---
"@jolly-pixel/network": minor
---

Add `ChannelTransport` and `ChannelTransportHost` (`@jolly-pixel/network/transport/channel.ts`) to relay client connections over a `BroadcastChannel`, `MessagePort` or worker.
