---
"@jolly-pixel/controls": minor
"@jolly-pixel/runtime": minor
---

Add `Mouse.scroll` for signed wheel magnitude and keep drags alive once the
cursor leaves the canvas. `Runtime.load()`/`configureRuntimeDevice` accept
`maxFps` to override the GPU-benchmarked render cap.
