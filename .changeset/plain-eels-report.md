---
"@jolly-pixel/ui": minor
---

Add `jolly-log`, a capped feed of short status messages, with a DOM-free `LogQueue` owning the cap and the per-entry grace period.
Wire it into the voxel-map viewport for peer joins, peer departures and camera mode changes.
