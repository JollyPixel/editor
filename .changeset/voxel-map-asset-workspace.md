---
"@jolly-pixel/voxel.renderer": major
---

Move voxel-map persistence, collaboration, and Tiled asset loading into `@jolly-pixel/asset.voxel-map`.
Move `VoxelRenderer` there too, and remove the renderer's `/asset` and `/network` exports.
