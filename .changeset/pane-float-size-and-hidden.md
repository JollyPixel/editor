---
"@jolly-pixel/ui": minor
---

Add `floatWidth`/`floatHeight` to `jolly-pane` to size the window a pane opens when first dragged out of its dock; hidden tabs now fall back to their group size instead of 160x80.
Add `hidden`, `floatWidth` and `floatHeight` options to the `Pane` facade.
