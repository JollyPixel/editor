---
"@jolly-pixel/pixel-draw.renderer": minor
---

Add `UVRegion.toLayout()` and `UVRegion.fromLayout(layout, identity)`, with the `UVLayoutData` and `UVRegionIdentity` types, to split a UV region's geometry from its identity.
