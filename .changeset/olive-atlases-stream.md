---
"@jolly-pixel/voxel.renderer": minor
---

Add `padAtlasRegion` and `TilesetManager.updateSourceRegion` to repad only the tiles a dirty rectangle touches, making per-frame atlas updates affordable.
