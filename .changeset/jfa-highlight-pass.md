---
"@jolly-pixel/three": minor
---

Added `HighlightPassJfa`, a Jump Flood Algorithm-based alternative to `HighlightPass` (hardened from
the `examples/` research prototype, now with `InstancedMesh` support), selectable via the new
`"highlightJfa"` `SelectionTechnique`.
