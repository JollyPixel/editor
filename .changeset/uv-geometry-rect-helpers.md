---
"@jolly-pixel/pixel-draw.renderer": minor
---

Export `rectOf()`, `triangleCornerOf()` and `withRotation()`, the UV geometry helpers for reading a geometry's bounding rect and triangle corner and setting its quarter-turn rotation.
