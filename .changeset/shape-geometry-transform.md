---
"@jolly-pixel/voxel.renderer": minor
---

`buildShapeGeometry()` accepts an optional `VoxelTransform` and returns the geometry oriented
like the chunk mesher places it (rotation, flips, flipY winding).
