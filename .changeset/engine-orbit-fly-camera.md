---
"@jolly-pixel/engine": minor
---

Add `OrbitFlyCamera`, a WASD + mouse-look camera with an optional `"lock"` (fixed-pivot orbit) or `"elastic"` (trailing pivot) focus mode, consolidated from voxel-map and voxel-model's near-identical copies.
