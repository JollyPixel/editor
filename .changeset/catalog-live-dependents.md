---
"@jolly-pixel/asset-server": minor
---

Add `liveDependentsOf(assetId)` to `CatalogClient` and `CatalogProjection`: the dependents that still have a record, the same assets delete protection counts.
