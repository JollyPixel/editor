---
"@jolly-pixel/engine": minor
---

`OrbitFlyCameraOptions` now extends `CameraOptions`, so `near`, `far`, `viewport`, `depth`, `postProcessing` and the other camera options can be set at creation.
