---
"@jolly-pixel/editor.pixel-art": minor
---

Add light/dark theming: `<pixel-draw-panel>` gets a `theme` attribute/property (`"light" | "dark" | "auto"`, default `"auto"`, follows `prefers-color-scheme`), with WCAG AA-checked palettes in `theme.ts`.
