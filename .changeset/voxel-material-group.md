---
"@jolly-pixel/voxel.renderer": minor
---

Add a `materialGroup` block surface option: grouped blocks get their own chunk material on a shared atlas, and `materialCustomizer` reads it from `surface.materialGroup`.
