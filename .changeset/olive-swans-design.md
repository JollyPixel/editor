---
"@jolly-pixel/controls": minor
"@jolly-pixel/engine": minor
---

Add `AxisMap`, `Axis`, and normalized axis sources for named movement input.
Export `AtomicInput` and its action types, including `ANY` and `NONE` keyboard
sentinels.
