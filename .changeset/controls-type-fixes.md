---
"@jolly-pixel/controls": patch
---

Accept lowercase letter shorthands and mouse `"ANY"`/`"NONE"` sentinels in types, validate the state segment in `isCombinedAction()`, and exclude sentinels from `InputActionQuery.value`.
