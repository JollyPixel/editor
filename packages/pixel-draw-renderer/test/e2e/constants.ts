export const PORT = 3000;
export const BASE_URL = `http://localhost:${PORT}`;

// Must match examples/scripts/main.ts's `texture.size` passed to
// drawPanel.initialize().
export const TEXTURE_SIZE = {
  x: 80,
  y: 80
};
