// Import Internal Dependencies
import { createSimpleExample } from "../shared/example.ts";
import { tabs } from "../shared/containerBuilders.ts";

export const TAB_EXAMPLE = createSimpleExample(
  "containers/tab",
  "Tab",
  "Containers",
  tabs
);
