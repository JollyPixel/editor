// Import Third-party Dependencies
import { pixelArtModelKind } from "@jolly-pixel/asset.pixel-art/network/client.ts";

// CONSTANTS
export const DEMO_TEXTURE_KIND = pixelArtModelKind({
  history: {
    enabled: true
  }
});
