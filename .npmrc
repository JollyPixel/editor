; pnpm is the package manager for this repository; its settings live in
; pnpm-workspace.yaml. The keys below only matter if someone reaches for npm.
save-exact=true
save-prefix=
allow-git=none
ignore-scripts=true
