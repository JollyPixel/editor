{
  "name": "@jolly-pixel/tree-view",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/TreeView.class.js",
  "types": "dist/TreeView.class.d.ts",
  "scripts": {
    "prepublish": "rimraf ./dist && tsc -b",
    "test": "tsx --test test/**/*.test.ts",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "keywords": [],
  "files": [
    "dist"
  ]
}
